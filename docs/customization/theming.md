# Appearance Settings

The Qt app provides built-in appearance mode and visual-density settings. It does not load user CSS, JSON keymaps, or renderer snippets. Those mechanisms belonged to the retired Electron app.

Use Settings for supported visual configuration; do not place CSS or extension files in the application-data directory expecting them to execute.
