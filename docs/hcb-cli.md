# HCB CLI Status

No `hcb` CLI ships with the current C++/Qt macOS app. Commands documented for the former JavaScript/Electron CLI are retired and must not be used as implementation or support guidance.

The current supported interfaces are the macOS desktop app, its SQLite-backed local cache, and Google Calendar/Tasks synchronization.
