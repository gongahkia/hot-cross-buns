# CLI/MCP Workflow Status

This workflow is retired. The native macOS app currently has no bundled CLI or MCP listener. Do not run `pnpm hcb` or rely on prior confirmation/vault commands.

Future agent access requires a separately approved implementation after macOS product acceptance.
