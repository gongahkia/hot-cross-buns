name: Release

on:
  workflow_dispatch:

jobs:
  apple-release-placeholder:
    name: Apple Release Placeholder
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Explain current release status
        run: |
          echo "The Tauri desktop and server release pipelines are deprecated."
          echo "apps/apple now contains the SwiftUI rewrite shell."
          echo "Add Developer ID signing, notarization, and DMG packaging after Google OAuth configuration is in place."
