Read `CODEX_MEGASTRUCTURE_HANDOFF.md`, `docs/megastructure-design-spec.md`, `docs/megastructure-technical-design.md`, the selected open GitHub megastructure roadmap issue, and all existing authoritative project documentation.

First inspect the repository and run the existing baseline validation. Then complete only the selected GitHub issue.

Preserve all existing deterministic-generation, streaming, collision-handoff, profiling, and normal-frame heavyweight mutual-deferral behavior. Do not broadly refactor unrelated code.

For each coherent completed task:
1. update the GitHub issue;
2. run the relevant validation;
3. make a focused git commit.

Stop when the selected issue acceptance criteria are satisfied. Leave the worktree clean and report:
- baseline and final validation results;
- exact files changed;
- commits made;
- discovered integration points;
- any genuinely blocking owner decisions required by the next issue.
