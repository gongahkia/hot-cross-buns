# PostgreSQL
DB_PASSWORD=changeme
DATABASE_URL=postgres://hotcrossbuns:changeme@localhost:5432/hotcrossbuns?sslmode=disable

# Server
PORT=8080
AUTH_REQUIRED=false
MAGIC_LINK_SECRET=change-this-to-a-32-char-secret!

# SMTP (required for magic link auth)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# CORS
CORS_ORIGINS=http://localhost:1420
