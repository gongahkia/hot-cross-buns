# PostgreSQL
DB_PASSWORD=changeme
DATABASE_URL=postgres://tickclone:changeme@localhost:5432/tickclone?sslmode=disable

# Server
PORT=8080
AUTH_REQUIRED=false
MAGIC_LINK_SECRET=change-this-to-a-32-char-secret!

# SMTP (required for magic link auth)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# CORS
CORS_ORIGINS=*
