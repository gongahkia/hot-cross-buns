# Cache fixture

This document is a tiny on-disk cache produced by an idealised export from a
Melon Pan install. The macOS runtime must read it byte-identically: the layout
is `docs/<id>/{current.md,current.docs.json,meta.json}` plus
`snapshots/<id>/<rev>.{md,docs.json}`.

- A bullet line.
- **Bold** and *italic* survive.

| col1 | col2 |
| --- | --- |
| a | b |
