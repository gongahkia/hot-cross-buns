import SwiftUI

// TODO: prune — dead file kept only because removal requires a pbxproj
// edit. Retired with the sidebar refactor that split "Store" into Tasks +
// Notes.
// The week-view task drawer was removed; undated tasks now live in the
// dedicated Notes tab. File kept as a stub so the checked-in .xcodeproj
// reference still resolves without a full pbxproj regeneration.
struct TaskDrawerPanel: View {
    var body: some View { EmptyView() }
}
